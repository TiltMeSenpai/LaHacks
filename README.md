A web app created in 36 hours for the 2014 LAHacks competition that allows you to create unit tests for python and (almost) Java files and test your code.
To install

```bash
chmod +x startup.sh setup.sh && setup.sh
```

To run

```bash
startup.sh
```
